# ============================================================================
# Bug Tracking System - Database Configuration
# ============================================================================
# Update the credentials below to match your local MySQL server setup.

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/bug_tracking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=
